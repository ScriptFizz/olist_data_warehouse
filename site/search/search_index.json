{"config":{"indexing":"full","lang":["en"],"min_search_length":3,"prebuild_index":false,"separator":"[\\s\\-]+"},"docs":[{"location":"","text":"Welcome to MkDocs For full documentation visit mkdocs.org . Commands mkdocs new [dir-name] - Create a new project. mkdocs serve - Start the live-reloading docs server. mkdocs build - Build the documentation site. mkdocs -h - Print help message and exit. Project layout mkdocs.yml # The configuration file. docs/ index.md # The documentation homepage. ... # Other markdown pages, images and other files.","title":"Home"},{"location":"#welcome-to-mkdocs","text":"For full documentation visit mkdocs.org .","title":"Welcome to MkDocs"},{"location":"#commands","text":"mkdocs new [dir-name] - Create a new project. mkdocs serve - Start the live-reloading docs server. mkdocs build - Build the documentation site. mkdocs -h - Print help message and exit.","title":"Commands"},{"location":"#project-layout","text":"mkdocs.yml # The configuration file. docs/ index.md # The documentation homepage. ... # Other markdown pages, images and other files.","title":"Project layout"},{"location":"engineering/cli/extract_cli/","text":"Extract CLI This section documents the extraction command line code Data ingestion cli ::: src.cli.extract_csv_cli Exchange rates cli ::: src.cli.extract_rates_cli","title":"Extract command"},{"location":"engineering/cli/extract_cli/#extract-cli","text":"This section documents the extraction command line code","title":"Extract CLI"},{"location":"engineering/cli/extract_cli/#data-ingestion-cli","text":"::: src.cli.extract_csv_cli","title":"Data ingestion cli"},{"location":"engineering/cli/extract_cli/#exchange-rates-cli","text":"::: src.cli.extract_rates_cli","title":"Exchange rates cli"},{"location":"engineering/cli/load_cli/","text":"Load CLI This section documents the loading command line code. Load data to bigquery raw dataset ::: src.cli.load_bigquery_cli","title":"Load command"},{"location":"engineering/cli/load_cli/#load-cli","text":"This section documents the loading command line code.","title":"Load CLI"},{"location":"engineering/cli/load_cli/#load-data-to-bigquery-raw-dataset","text":"::: src.cli.load_bigquery_cli","title":"Load data to bigquery raw dataset"},{"location":"engineering/cli/transform_cli/","text":"Transform CLI This section documents the transform command line code. Data processing (cli) ::: src.cli.transform_cli","title":"Transform command"},{"location":"engineering/cli/transform_cli/#transform-cli","text":"This section documents the transform command line code.","title":"Transform CLI"},{"location":"engineering/cli/transform_cli/#data-processing-cli","text":"::: src.cli.transform_cli","title":"Data processing (cli)"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/","text":"ADR 001: PostgreSQL raw loading strategy Status Implemented. Context The Olist source is distributed as a static collection of complete CSV files, but the portfolio pipeline should also demonstrate credible incremental batch behavior. Applying one loading strategy to every table would be incorrect: some tables have reliable business keys and mutable records; reviews do not have a trustworthy unique source key; geolocation and translation behave like reference snapshots; an absent row in a delta batch does not necessarily represent deletion. Decision PostgreSQL raw tables use table-specific loading strategies. Table Strategy Identity customers upsert customer_id orders upsert order_id order_items upsert order_id, order_item_id payments upsert order_id, sequential products upsert product_id sellers upsert seller_id reviews append and deduplicate deterministic record hash geolocation snapshot replacement complete delivered snapshot translation snapshot replacement complete delivered snapshot The original Olist CSV collection is treated as a bootstrap snapshot. Later loads may contain new or changed records for upsert-managed tables. Every published row contains: _batch_id ; _ingested_at ; _record_hash . Each run is recorded in a separate ingestion audit table. Idempotency For upsert tables: unseen business keys are inserted; existing keys with changed record hashes are updated; existing keys with unchanged hashes are no-ops. For append-deduplicated tables: unseen record hashes are inserted; previously loaded hashes are ignored. For snapshot-replacement tables: the complete table is replaced transactionally. Replaying an identical batch does not create duplicates or change its existing row metadata. Failure recovery Raw-table staging and publication occur in one PostgreSQL transaction. The audit ledger uses separate committed transactions so that failure evidence survives a raw publication rollback. If any table fails: no target table changes are committed; the ingestion run is recorded as failed in a separate failure-handling step; staging relations created by the failed transaction disappear on rollback. Deletions Incremental batches do not infer deletion from absence. Deletion requires either: an explicit source tombstone; or reconciliation against a later complete snapshot. The initial implementation supports inserts and updates. Tombstone processing is future work. dbt impact Incremental dbt models must use affected business keys or _ingested_at ; they must not rely only on the maximum business event date. This allows late changes to historical orders to be processed. Consequences Benefits: loading semantics reflect each table's real grain; retries are idempotent; late updates can propagate downstream; ingestion runs and row changes are observable. Costs: schema metadata and merge logic are more complex; snapshot deletion and delta deletion require distinct handling; reviews require hash-based identity rather than a conventional primary key.","title":"PostgreSQL raw loading"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#adr-001-postgresql-raw-loading-strategy","text":"","title":"ADR 001: PostgreSQL raw loading strategy"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#status","text":"Implemented.","title":"Status"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#context","text":"The Olist source is distributed as a static collection of complete CSV files, but the portfolio pipeline should also demonstrate credible incremental batch behavior. Applying one loading strategy to every table would be incorrect: some tables have reliable business keys and mutable records; reviews do not have a trustworthy unique source key; geolocation and translation behave like reference snapshots; an absent row in a delta batch does not necessarily represent deletion.","title":"Context"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#decision","text":"PostgreSQL raw tables use table-specific loading strategies. Table Strategy Identity customers upsert customer_id orders upsert order_id order_items upsert order_id, order_item_id payments upsert order_id, sequential products upsert product_id sellers upsert seller_id reviews append and deduplicate deterministic record hash geolocation snapshot replacement complete delivered snapshot translation snapshot replacement complete delivered snapshot The original Olist CSV collection is treated as a bootstrap snapshot. Later loads may contain new or changed records for upsert-managed tables. Every published row contains: _batch_id ; _ingested_at ; _record_hash . Each run is recorded in a separate ingestion audit table.","title":"Decision"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#idempotency","text":"For upsert tables: unseen business keys are inserted; existing keys with changed record hashes are updated; existing keys with unchanged hashes are no-ops. For append-deduplicated tables: unseen record hashes are inserted; previously loaded hashes are ignored. For snapshot-replacement tables: the complete table is replaced transactionally. Replaying an identical batch does not create duplicates or change its existing row metadata.","title":"Idempotency"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#failure-recovery","text":"Raw-table staging and publication occur in one PostgreSQL transaction. The audit ledger uses separate committed transactions so that failure evidence survives a raw publication rollback. If any table fails: no target table changes are committed; the ingestion run is recorded as failed in a separate failure-handling step; staging relations created by the failed transaction disappear on rollback.","title":"Failure recovery"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#deletions","text":"Incremental batches do not infer deletion from absence. Deletion requires either: an explicit source tombstone; or reconciliation against a later complete snapshot. The initial implementation supports inserts and updates. Tombstone processing is future work.","title":"Deletions"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#dbt-impact","text":"Incremental dbt models must use affected business keys or _ingested_at ; they must not rely only on the maximum business event date. This allows late changes to historical orders to be processed.","title":"dbt impact"},{"location":"engineering/decisions/001-postgres-raw-loading-strategy/#consequences","text":"Benefits: loading semantics reflect each table's real grain; retries are idempotent; late updates can propagate downstream; ingestion runs and row changes are observable. Costs: schema metadata and merge logic are more complex; snapshot deletion and delta deletion require distinct handling; reviews require hash-based identity rather than a conventional primary key.","title":"Consequences"},{"location":"engineering/etl/","text":"ETL Pipelines This section documents the ETL pipelines used to populate bigquery datasets. Stages Extract : Load raw data from source systems. Transform : Build analytics-ready models Load : Persist curated data to BigQuery Each stage is documented separately","title":"Overview"},{"location":"engineering/etl/#etl-pipelines","text":"This section documents the ETL pipelines used to populate bigquery datasets.","title":"ETL Pipelines"},{"location":"engineering/etl/#stages","text":"Extract : Load raw data from source systems. Transform : Build analytics-ready models Load : Persist curated data to BigQuery Each stage is documented separately","title":"Stages"},{"location":"engineering/etl/extract/","text":"Extract Pipeline This section documents all extraction stages ::: src.etl.extract.extract_csv ::: src.etl.extract.extract_exchange_rates","title":"Extraction"},{"location":"engineering/etl/extract/#extract-pipeline","text":"This section documents all extraction stages ::: src.etl.extract.extract_csv ::: src.etl.extract.extract_exchange_rates","title":"Extract Pipeline"},{"location":"engineering/etl/load/","text":"Load Pipeline This section documents all load stages. Pandera to BigQuery schema conversion ::: src.etl.load.pandera_to_bq Load processed data to raw dataset ::: src.etl.load.load_bigquery Create views/tables for core/analytics/bi datasets ::: src.etl.load.create_datasets","title":"Loading"},{"location":"engineering/etl/load/#load-pipeline","text":"This section documents all load stages.","title":"Load Pipeline"},{"location":"engineering/etl/load/#pandera-to-bigquery-schema-conversion","text":"::: src.etl.load.pandera_to_bq","title":"Pandera to BigQuery schema conversion"},{"location":"engineering/etl/load/#load-processed-data-to-raw-dataset","text":"::: src.etl.load.load_bigquery","title":"Load processed data to raw dataset"},{"location":"engineering/etl/load/#create-viewstables-for-coreanalyticsbi-datasets","text":"::: src.etl.load.create_datasets","title":"Create views/tables for core/analytics/bi datasets"},{"location":"engineering/etl/postgres_ingestion/","text":"PostgreSQL ingestion and publication This document explains how a validated group of Olist tables becomes a safe, auditable PostgreSQL raw-layer batch. It is a learning guide to the current implementation rather than a list of future intentions. The main idea A load is treated as one batch containing all nine processed Olist tables. The pipeline first validates every table, assigns the batch an identity, stages all rows, and only then changes the public raw tables. processed CSV files | v Pandera and dataset validation | v IngestionBatch + RUNNING audit row | v COPY every table into private staging tables | v publish every table in one transaction | | success failure | | commit raw tables rollback raw tables | | SUCCEEDED audit row FAILED audit row The audit writes use independent database transactions. This separation is important: if the raw transaction fails, its rollback must not erase the record that explains that an ingestion attempt failed. Responsibilities by component olist_tables.py : the table registry TABLES is the central declaration of what each Olist dataset means to the pipeline. Each TableConfig connects a filename, its Pandera schemas, its transformation function, and its publication strategy. This prevents the CLI and database loader from maintaining separate lists of tables and gradually disagreeing with one another. tables.py : loading policy types LoadStrategy defines the three supported publication behaviors. TableConfig validates policy at program startup. For example, an UPSERT table must declare a business key, while a snapshot table must not declare one. These checks turn configuration mistakes into early Python errors instead of incorrect database loads. ingestion_batch.py : batch and record identity IngestionBatch describes one execution attempt: batch_id is a new UUID for every attempt; source_fingerprint identifies the input content; started_at supplies a consistent UTC timestamp; row and table counts describe the input volume. Two retries of identical files have different batch IDs but the same source fingerprint. That distinction answers two different questions: \u201cWhich run did this?\u201d and \u201cWas its input the same?\u201d compute_record_hashes() creates a SHA-256 identity from each row's source values. Metadata columns are excluded so a retry produces the same record hash. compute_batch_fingerprint() combines sorted table names and sorted record hashes, making the result independent of dataframe and table iteration order. postgres_audit.py : the durable run ledger The ingestion_runs table records running , succeeded , or failed for each batch. start_ingestion_run() commits running before raw publication begins. finish_ingestion_run() later records completion and, for failures, only the exception type. It intentionally avoids storing exception messages that could contain credentials or sensitive source values. postgres.py : physical database loading load_tables_to_postgres() owns database mechanics, not source-file reading or business validation. It: checks that dataframes and registry entries match; acquires a transaction-scoped advisory lock, preventing overlapping writers from publishing to the same raw schema; creates target tables and required unique indexes; adds _batch_id , _ingested_at , and _record_hash to copied data; uses PostgreSQL COPY to load uniquely named staging tables efficiently; publishes each staging table according to its configured strategy; drops staging tables and commits everything together. The context-managed Psycopg connection commits when the block succeeds and rolls back when an exception escapes. Staging tables are created in that same transaction, so a rollback removes them too. load_postgres_cli.py : application workflow The CLI coordinates the components. It reads configuration and files, performs Pandera and cross-table validation, creates the batch, manages the audit lifecycle, and invokes the database loader. It does not contain SQL publication rules; those remain testable inside the loader. The three publication strategies UPSERT Customers, orders, order items, payments, products, and sellers have reliable business keys. New keys are inserted. Existing keys are updated only when the record hash changed. Records absent from a delta batch remain in the target. This supports late corrections without interpreting absence as deletion. Append and deduplicate Reviews lack a sufficiently reliable source key, so the complete record hash acts as technical identity. New hashes are appended and existing hashes are ignored. An exact retry therefore creates no duplicate review. This policy does not claim that two distinct review versions represent an update; both can remain as source observations, and dbt selects the analytical review version deterministically downstream. Snapshot replacement Geolocation and category translation are reference snapshots. Their staging contents replace the target contents with transactional TRUNCATE + INSERT . The table itself is not dropped, so its database identity and downstream dependencies remain stable. Why staging is necessary Publishing one table immediately after reading it could leave a mixed state: new customers but old orders, for example. Here, all COPY operations finish before publication begins, and every publication occurs in one transaction. If table nine fails, changes planned for tables one through eight are rolled back. The previous coherent raw dataset remains visible. What idempotency means here Safe re-execution depends on strategy: unchanged UPSERT rows are no-ops because their record hashes match; append-managed rows conflict on _record_hash and are ignored; snapshot tables are replaced with the same content; every attempt still receives its own audit row. Idempotency means repeated input does not corrupt or duplicate business data. It does not mean executions become invisible: the audit ledger deliberately records every attempt. Current deletion boundary An incremental file that omits an order does not prove that the order was deleted. The loader therefore retains absent UPSERT records. A future source would need explicit tombstones or an intentionally declared complete-snapshot reconciliation process before deletions could be applied safely. Local schema compatibility The metadata columns changed the physical raw-table shape. A local raw schema created by the older snapshot-only loader should be recreated once before using this implementation. This is a development migration, not a command the loader should silently perform against an unknown environment. After that one-time reset, subsequent loads preserve target tables and are safe to rerun. Integration tests avoid this issue by creating isolated schemas for every test and dropping them afterward. Incremental dbt facts The PostgreSQL loader incrementally maintains the raw layer, while dbt incrementally publishes the two fact tables. Each fact row contains warehouse_updated_at , calculated from the ingestion timestamps of every source record that can affect that row. During an incremental run, dbt compares each candidate row with the existing fact row having the same natural key: missing keys are inserted; keys with newer source lineage are updated; unchanged or older candidates are ignored. fct_orders merges on order_id . fct_order_items merges on the composite key (order_id, order_item_id) . This supports late changes to historical orders because selection is based on source ingestion time rather than order purchase date. The current intermediate models are views, so PostgreSQL still evaluates their source queries during an incremental run. The optimization primarily reduces fact-table writes rather than guaranteeing that the warehouse scans only new raw records.","title":"PostgreSQL ingestion"},{"location":"engineering/etl/postgres_ingestion/#postgresql-ingestion-and-publication","text":"This document explains how a validated group of Olist tables becomes a safe, auditable PostgreSQL raw-layer batch. It is a learning guide to the current implementation rather than a list of future intentions.","title":"PostgreSQL ingestion and publication"},{"location":"engineering/etl/postgres_ingestion/#the-main-idea","text":"A load is treated as one batch containing all nine processed Olist tables. The pipeline first validates every table, assigns the batch an identity, stages all rows, and only then changes the public raw tables. processed CSV files | v Pandera and dataset validation | v IngestionBatch + RUNNING audit row | v COPY every table into private staging tables | v publish every table in one transaction | | success failure | | commit raw tables rollback raw tables | | SUCCEEDED audit row FAILED audit row The audit writes use independent database transactions. This separation is important: if the raw transaction fails, its rollback must not erase the record that explains that an ingestion attempt failed.","title":"The main idea"},{"location":"engineering/etl/postgres_ingestion/#responsibilities-by-component","text":"","title":"Responsibilities by component"},{"location":"engineering/etl/postgres_ingestion/#olist_tablespy-the-table-registry","text":"TABLES is the central declaration of what each Olist dataset means to the pipeline. Each TableConfig connects a filename, its Pandera schemas, its transformation function, and its publication strategy. This prevents the CLI and database loader from maintaining separate lists of tables and gradually disagreeing with one another.","title":"olist_tables.py: the table registry"},{"location":"engineering/etl/postgres_ingestion/#tablespy-loading-policy-types","text":"LoadStrategy defines the three supported publication behaviors. TableConfig validates policy at program startup. For example, an UPSERT table must declare a business key, while a snapshot table must not declare one. These checks turn configuration mistakes into early Python errors instead of incorrect database loads.","title":"tables.py: loading policy types"},{"location":"engineering/etl/postgres_ingestion/#ingestion_batchpy-batch-and-record-identity","text":"IngestionBatch describes one execution attempt: batch_id is a new UUID for every attempt; source_fingerprint identifies the input content; started_at supplies a consistent UTC timestamp; row and table counts describe the input volume. Two retries of identical files have different batch IDs but the same source fingerprint. That distinction answers two different questions: \u201cWhich run did this?\u201d and \u201cWas its input the same?\u201d compute_record_hashes() creates a SHA-256 identity from each row's source values. Metadata columns are excluded so a retry produces the same record hash. compute_batch_fingerprint() combines sorted table names and sorted record hashes, making the result independent of dataframe and table iteration order.","title":"ingestion_batch.py: batch and record identity"},{"location":"engineering/etl/postgres_ingestion/#postgres_auditpy-the-durable-run-ledger","text":"The ingestion_runs table records running , succeeded , or failed for each batch. start_ingestion_run() commits running before raw publication begins. finish_ingestion_run() later records completion and, for failures, only the exception type. It intentionally avoids storing exception messages that could contain credentials or sensitive source values.","title":"postgres_audit.py: the durable run ledger"},{"location":"engineering/etl/postgres_ingestion/#postgrespy-physical-database-loading","text":"load_tables_to_postgres() owns database mechanics, not source-file reading or business validation. It: checks that dataframes and registry entries match; acquires a transaction-scoped advisory lock, preventing overlapping writers from publishing to the same raw schema; creates target tables and required unique indexes; adds _batch_id , _ingested_at , and _record_hash to copied data; uses PostgreSQL COPY to load uniquely named staging tables efficiently; publishes each staging table according to its configured strategy; drops staging tables and commits everything together. The context-managed Psycopg connection commits when the block succeeds and rolls back when an exception escapes. Staging tables are created in that same transaction, so a rollback removes them too.","title":"postgres.py: physical database loading"},{"location":"engineering/etl/postgres_ingestion/#load_postgres_clipy-application-workflow","text":"The CLI coordinates the components. It reads configuration and files, performs Pandera and cross-table validation, creates the batch, manages the audit lifecycle, and invokes the database loader. It does not contain SQL publication rules; those remain testable inside the loader.","title":"load_postgres_cli.py: application workflow"},{"location":"engineering/etl/postgres_ingestion/#the-three-publication-strategies","text":"","title":"The three publication strategies"},{"location":"engineering/etl/postgres_ingestion/#upsert","text":"Customers, orders, order items, payments, products, and sellers have reliable business keys. New keys are inserted. Existing keys are updated only when the record hash changed. Records absent from a delta batch remain in the target. This supports late corrections without interpreting absence as deletion.","title":"UPSERT"},{"location":"engineering/etl/postgres_ingestion/#append-and-deduplicate","text":"Reviews lack a sufficiently reliable source key, so the complete record hash acts as technical identity. New hashes are appended and existing hashes are ignored. An exact retry therefore creates no duplicate review. This policy does not claim that two distinct review versions represent an update; both can remain as source observations, and dbt selects the analytical review version deterministically downstream.","title":"Append and deduplicate"},{"location":"engineering/etl/postgres_ingestion/#snapshot-replacement","text":"Geolocation and category translation are reference snapshots. Their staging contents replace the target contents with transactional TRUNCATE + INSERT . The table itself is not dropped, so its database identity and downstream dependencies remain stable.","title":"Snapshot replacement"},{"location":"engineering/etl/postgres_ingestion/#why-staging-is-necessary","text":"Publishing one table immediately after reading it could leave a mixed state: new customers but old orders, for example. Here, all COPY operations finish before publication begins, and every publication occurs in one transaction. If table nine fails, changes planned for tables one through eight are rolled back. The previous coherent raw dataset remains visible.","title":"Why staging is necessary"},{"location":"engineering/etl/postgres_ingestion/#what-idempotency-means-here","text":"Safe re-execution depends on strategy: unchanged UPSERT rows are no-ops because their record hashes match; append-managed rows conflict on _record_hash and are ignored; snapshot tables are replaced with the same content; every attempt still receives its own audit row. Idempotency means repeated input does not corrupt or duplicate business data. It does not mean executions become invisible: the audit ledger deliberately records every attempt.","title":"What idempotency means here"},{"location":"engineering/etl/postgres_ingestion/#current-deletion-boundary","text":"An incremental file that omits an order does not prove that the order was deleted. The loader therefore retains absent UPSERT records. A future source would need explicit tombstones or an intentionally declared complete-snapshot reconciliation process before deletions could be applied safely.","title":"Current deletion boundary"},{"location":"engineering/etl/postgres_ingestion/#local-schema-compatibility","text":"The metadata columns changed the physical raw-table shape. A local raw schema created by the older snapshot-only loader should be recreated once before using this implementation. This is a development migration, not a command the loader should silently perform against an unknown environment. After that one-time reset, subsequent loads preserve target tables and are safe to rerun. Integration tests avoid this issue by creating isolated schemas for every test and dropping them afterward.","title":"Local schema compatibility"},{"location":"engineering/etl/postgres_ingestion/#incremental-dbt-facts","text":"The PostgreSQL loader incrementally maintains the raw layer, while dbt incrementally publishes the two fact tables. Each fact row contains warehouse_updated_at , calculated from the ingestion timestamps of every source record that can affect that row. During an incremental run, dbt compares each candidate row with the existing fact row having the same natural key: missing keys are inserted; keys with newer source lineage are updated; unchanged or older candidates are ignored. fct_orders merges on order_id . fct_order_items merges on the composite key (order_id, order_item_id) . This supports late changes to historical orders because selection is based on source ingestion time rather than order purchase date. The current intermediate models are views, so PostgreSQL still evaluates their source queries during an incremental run. The optimization primarily reduces fact-table writes rather than guaranteeing that the warehouse scans only new raw records.","title":"Incremental dbt facts"},{"location":"engineering/etl/transform/","text":"Transform Pipeline This section documents all transform stages. Schema validation ::: src.etl.transform.raw_schemas Data processing stage ::: src.etl.transform.transform_data","title":"Transformation"},{"location":"engineering/etl/transform/#transform-pipeline","text":"This section documents all transform stages.","title":"Transform Pipeline"},{"location":"engineering/etl/transform/#schema-validation","text":"::: src.etl.transform.raw_schemas","title":"Schema validation"},{"location":"engineering/etl/transform/#data-processing-stage","text":"::: src.etl.transform.transform_data","title":"Data processing stage"},{"location":"engineering/runtime/docker/","text":"Reproducible container execution The project provides a reusable application image containing the packaged Python pipeline and dbt adapters. PostgreSQL runs as a separate Compose service. BigQuery remains external and is not containerized. Services postgres Runs PostgreSQL 16 and persists warehouse data in the postgres_data named volume. From the host: localhost:5433 From another Compose container: postgres:5432 pipeline A short-lived command environment containing: the installed olist_dw Python package; the ETL command-line applications; dbt Core; the PostgreSQL and BigQuery dbt adapters. The service belongs to the tools Compose profile because it is used for one-off commands rather than as a continuously running process. Initial setup Create the local environment file: cp .env.example .env Set at least: POSTGRES_DB=olist POSTGRES_USER=olist POSTGRES_PASSWORD=replace-with-a-local-password POSTGRES_PORT=5433 KAGGLE_USERNAME=your-kaggle-username KAGGLE_KEY=your-kaggle-api-key Do not commit .env or real credentials. On Linux, export the host user's numeric identifiers so files created through bind mounts remain owned by the host user: export LOCAL_UID=\"$(id -u)\" export LOCAL_GID=\"$(id -g)\" These values may also be added to .env . Build and validate the runtime Validate the Compose configuration: docker compose config --quiet Build the pipeline image: docker compose build pipeline Run the lightweight container smoke test: ./scripts/container_smoke_test.sh The smoke test verifies: Compose configuration; image construction; installation of the Python package; installation of dbt; PostgreSQL readiness; connectivity from the pipeline container. It intentionally does not download the Kaggle dataset. Run the complete local pipeline Start PostgreSQL: docker compose up -d postgres Download the raw source files: docker compose run --rm --no-deps pipeline \\ olist-extract \\ --out-dir data/raw Transform and validate the source data: docker compose run --rm --no-deps pipeline \\ olist-transform \\ --raw-data-dir data/raw \\ --processed-data-dir data/processed Load the validated data into PostgreSQL: docker compose run --rm pipeline \\ olist-load-postgres \\ --processed-dir data/processed Build and test the dbt warehouse: docker compose run --rm pipeline \\ dbt build \\ --project-dir dbt \\ --profiles-dir dbt \\ --target postgres_dev The pipeline service receives PostgreSQL connection settings through environment variables and connects over the Compose network. Safe re-execution The commands are designed to be run again safely: transformation rewrites deterministic processed outputs; raw PostgreSQL publication uses table-specific loading strategies; a failed multi-table publication is rolled back; ingestion attempts are recorded in the audit schema; unchanged upsert records are not rewritten; dbt facts update incrementally from source watermarks. Re-running the complete workflow should not duplicate source records. Inspect the result List tables: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"\\dt raw.*\" Inspect dbt relations: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"\\dt dbt_dev.*\" Inspect ingestion runs: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"select * from pipeline_metadata.ingestion_runs order by started_at desc limit 10;\" Cleanup Stop the containers while preserving PostgreSQL data: docker compose down Removing the named volume also deletes the local PostgreSQL warehouse: docker compose down --volumes Use the second command only when a completely clean database is intentional. BigQuery target The same dbt project retains a bigquery_dev target. BigQuery credentials are not included in the image and must be supplied at runtime. PostgreSQL is the default local development target because it provides an inexpensive and reproducible development environment. BigQuery remains the optional cloud deployment target.","title":"Docker"},{"location":"engineering/runtime/docker/#reproducible-container-execution","text":"The project provides a reusable application image containing the packaged Python pipeline and dbt adapters. PostgreSQL runs as a separate Compose service. BigQuery remains external and is not containerized.","title":"Reproducible container execution"},{"location":"engineering/runtime/docker/#services","text":"","title":"Services"},{"location":"engineering/runtime/docker/#postgres","text":"Runs PostgreSQL 16 and persists warehouse data in the postgres_data named volume. From the host: localhost:5433 From another Compose container: postgres:5432","title":"postgres"},{"location":"engineering/runtime/docker/#pipeline","text":"A short-lived command environment containing: the installed olist_dw Python package; the ETL command-line applications; dbt Core; the PostgreSQL and BigQuery dbt adapters. The service belongs to the tools Compose profile because it is used for one-off commands rather than as a continuously running process.","title":"pipeline"},{"location":"engineering/runtime/docker/#initial-setup","text":"Create the local environment file: cp .env.example .env Set at least: POSTGRES_DB=olist POSTGRES_USER=olist POSTGRES_PASSWORD=replace-with-a-local-password POSTGRES_PORT=5433 KAGGLE_USERNAME=your-kaggle-username KAGGLE_KEY=your-kaggle-api-key Do not commit .env or real credentials. On Linux, export the host user's numeric identifiers so files created through bind mounts remain owned by the host user: export LOCAL_UID=\"$(id -u)\" export LOCAL_GID=\"$(id -g)\" These values may also be added to .env .","title":"Initial setup"},{"location":"engineering/runtime/docker/#build-and-validate-the-runtime","text":"Validate the Compose configuration: docker compose config --quiet Build the pipeline image: docker compose build pipeline Run the lightweight container smoke test: ./scripts/container_smoke_test.sh The smoke test verifies: Compose configuration; image construction; installation of the Python package; installation of dbt; PostgreSQL readiness; connectivity from the pipeline container. It intentionally does not download the Kaggle dataset.","title":"Build and validate the runtime"},{"location":"engineering/runtime/docker/#run-the-complete-local-pipeline","text":"Start PostgreSQL: docker compose up -d postgres Download the raw source files: docker compose run --rm --no-deps pipeline \\ olist-extract \\ --out-dir data/raw Transform and validate the source data: docker compose run --rm --no-deps pipeline \\ olist-transform \\ --raw-data-dir data/raw \\ --processed-data-dir data/processed Load the validated data into PostgreSQL: docker compose run --rm pipeline \\ olist-load-postgres \\ --processed-dir data/processed Build and test the dbt warehouse: docker compose run --rm pipeline \\ dbt build \\ --project-dir dbt \\ --profiles-dir dbt \\ --target postgres_dev The pipeline service receives PostgreSQL connection settings through environment variables and connects over the Compose network.","title":"Run the complete local pipeline"},{"location":"engineering/runtime/docker/#safe-re-execution","text":"The commands are designed to be run again safely: transformation rewrites deterministic processed outputs; raw PostgreSQL publication uses table-specific loading strategies; a failed multi-table publication is rolled back; ingestion attempts are recorded in the audit schema; unchanged upsert records are not rewritten; dbt facts update incrementally from source watermarks. Re-running the complete workflow should not duplicate source records.","title":"Safe re-execution"},{"location":"engineering/runtime/docker/#inspect-the-result","text":"List tables: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"\\dt raw.*\" Inspect dbt relations: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"\\dt dbt_dev.*\" Inspect ingestion runs: docker compose exec postgres \\ psql \\ -U \"$POSTGRES_USER\" \\ -d \"$POSTGRES_DB\" \\ -c \"select * from pipeline_metadata.ingestion_runs order by started_at desc limit 10;\"","title":"Inspect the result"},{"location":"engineering/runtime/docker/#cleanup","text":"Stop the containers while preserving PostgreSQL data: docker compose down Removing the named volume also deletes the local PostgreSQL warehouse: docker compose down --volumes Use the second command only when a completely clean database is intentional.","title":"Cleanup"},{"location":"engineering/runtime/docker/#bigquery-target","text":"The same dbt project retains a bigquery_dev target. BigQuery credentials are not included in the image and must be supplied at runtime. PostgreSQL is the default local development target because it provides an inexpensive and reproducible development environment. BigQuery remains the optional cloud deployment target.","title":"BigQuery target"},{"location":"metrics/customer_kpis/","text":"Customer KPIs This document defines customer-level KPIs used for analytics and BI. All metrics are computed at customer grain and evaluated as of snapshot date . Customer grain One row per customer_unique_id per snapshot_date . Metrics Recency (recency_days) Definition Number of days since customer's most recent completed purchase. Formula DATE_DIFF(snapshot_date, last_purchase_date, DAY) Frequency Definition Total number of distinct orders place by the customer. Formula COUNT(DISTINCT order_id) Monetary (monetary_value) Definition Total revenue generated by the customer. Formula SUM(total_order_value) is_inactive Definition Flag indicating wether the customer is inactive based on recency. Formula recency_days > inactivity_threshold days Interpretation TRUE=inactive, FALSE=active Monetary Segment (monetary_segment) Definition Customer value tier based on total spending. Rules < 100: Low value 100-500: Mid valud 500: High Value Behavior Segment (behavior_segment) Definition Behavioral classification based on purchasing pattern. Priority Logic 1. High-ticket planner 2. Impulse buyer 3. Freight-sensitive 4. Multi-item shopper 5. Standard buyer","title":"Customer KPIs"},{"location":"metrics/customer_kpis/#customer-kpis","text":"This document defines customer-level KPIs used for analytics and BI. All metrics are computed at customer grain and evaluated as of snapshot date .","title":"Customer KPIs"},{"location":"metrics/customer_kpis/#customer-grain","text":"One row per customer_unique_id per snapshot_date .","title":"Customer grain"},{"location":"metrics/customer_kpis/#metrics","text":"","title":"Metrics"},{"location":"metrics/customer_kpis/#recency-recency_days","text":"Definition Number of days since customer's most recent completed purchase. Formula DATE_DIFF(snapshot_date, last_purchase_date, DAY)","title":"Recency (recency_days)"},{"location":"metrics/customer_kpis/#frequency","text":"Definition Total number of distinct orders place by the customer. Formula COUNT(DISTINCT order_id)","title":"Frequency"},{"location":"metrics/customer_kpis/#monetary-monetary_value","text":"Definition Total revenue generated by the customer. Formula SUM(total_order_value)","title":"Monetary (monetary_value)"},{"location":"metrics/customer_kpis/#is_inactive","text":"Definition Flag indicating wether the customer is inactive based on recency. Formula recency_days > inactivity_threshold days Interpretation TRUE=inactive, FALSE=active","title":"is_inactive"},{"location":"metrics/customer_kpis/#monetary-segment-monetary_segment","text":"Definition Customer value tier based on total spending. Rules < 100: Low value 100-500: Mid valud 500: High Value","title":"Monetary Segment (monetary_segment)"},{"location":"metrics/customer_kpis/#behavior-segment-behavior_segment","text":"Definition Behavioral classification based on purchasing pattern. Priority Logic 1. High-ticket planner 2. Impulse buyer 3. Freight-sensitive 4. Multi-item shopper 5. Standard buyer","title":"Behavior Segment (behavior_segment)"}]}