version: 2

seeds:
  - name: brazil_states
    description: Authoritative reference for Brazilian state codes.
    columns:
      - name: state_code
        data_tests: [not_null, unique]

      - name: state_name
        data_tests: [not_null, unique]

      - name: state_iso_code 
        data_tests: [not_null, unique]